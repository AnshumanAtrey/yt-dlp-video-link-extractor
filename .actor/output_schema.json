{
  "actorOutputSchemaVersion": 1,
  "title": "yt-dlp Video Link Extractor Output",
  "description": "One record per resolved video: direct links plus metadata.",
  "type": "object",
  "properties": {
    "items": {
      "type": "string",
      "title": "Resolved links",
      "description": "Direct stream/download links and metadata for each input URL.",
      "template": "{{links.apiDefaultDatasetUrl}}/items"
    },
    "log": {
      "type": "string",
      "title": "Run log",
      "description": "Full execution log.",
      "template": "{{links.publicRunUrl}}/log"
    }
  }
}
